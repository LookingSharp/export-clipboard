# Repository instructions

Read and obey `/AGENTS.md` before modifying this repository.

In particular, every change must complete the specification, changelog,
Semantic Versioning, and test-impact checks defined there.

Do not treat those checks as optional simply because the requested task does
not mention them explicitly.
